# Configuración MongoDB
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=productosdb

# Configuración de mensajes internacionales
spring.messages.basename=messages

# Puerto del servidor
server.port=8080